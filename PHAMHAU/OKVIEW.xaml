<Window x:Class="PHAMHAU.OKVIEW"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PHAMHAU"
        mc:Ignorable="d"
        Title="OKVIEW" Height="450" Width="800">
    <Grid>
        <Border Background="#2C3E50" Height="50">
            <TextBlock Text="BIỂU ĐỒ LỰC CẮT"
               Foreground="White"
               FontSize="18"
               Margin="20,0"
               VerticalAlignment="Center"/>
        </Border>

        <Grid Margin="0,50,0,40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!-- INPUT -->
            <Border Background="White" Margin="10" Padding="15" CornerRadius="10">
                <StackPanel>

                    <TextBlock Text="L (m)"/>
                    <TextBox x:Name="txtL" Text="6"/>

                    <TextBlock Text="q (kN/m)"/>
                    <TextBox x:Name="txtQ" Text="10"/>

                    <Button Content="Cập nhật"
                    Margin="0,10,0,0"
                    Click="Update_Click"/>

                </StackPanel>
            </Border>

            <!-- CANVAS -->
            <Border Grid.Column="1" Background="White" Margin="10" CornerRadius="10">
                <Canvas x:Name="canvas"
                MouseLeftButtonDown="Canvas_Click"/>
            </Border>
        </Grid>

        <!-- STATUS -->
        <Border Background="#DDD" Height="35" VerticalAlignment="Bottom">
            <TextBlock x:Name="txtStatus" Margin="10,0"/>
        </Border>

    </Grid>
</Window>
